import GuestInfo from "../../organisms/GuestInfo/GuestInfo"

const Admin = () => {
    return (
        <main className="w-full h-full bg-white">
            <GuestInfo />
        </main>
    )
}

export default Admin